function calculate() {
    const shape = document.getElementById('shapes').value;
    // Add logic here based on shape and input values
    alert(`Calculating area for ${shape}`);
  }
  
  function calculateVolume() {
    const shape = document.getElementById('shapes').value;
    alert(`Calculating volume for ${shape}`);
  }
  
  function calculateRadius() {
    const shape = document.getElementById('shapes').value;
    alert(`Calculating radius for ${shape}`);
  }